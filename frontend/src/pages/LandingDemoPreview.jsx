// Standalone preview wrapper for LandingDemo
// Import this in App.jsx as a route to preview, or render directly
import LandingDemo from '../components/landing/LandingDemo';

export default function LandingDemoPreview() {
  return <LandingDemo />;
}
